
export interface alliedModalProvideChildren {
    children: JSX.Element | null
}

export interface alliedModalData {
    alliedName: string,
    alliedCompanyImg: string,
    isLoading: boolean,
    userNotFound: boolean
}